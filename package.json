{
  "name": "editor",
  "version": "0.1.0",
  "description": "Music Notation Editor POC with Cell-based architecture",
  "main": "src/js/main.js",
  "scripts": {
    "dev": "node src/js/dev-server.js",
    "build": "npm run build-css && npm run build-wasm && npm run build-js",
    "build-wasm": "wasm-pack build . --target web --out-dir dist/pkg",
    "build-js": "rollup -c",
    "build-css": "./build-css.sh",
    "build-prod": "npm run build-css && npm run build-wasm -- --release && npm run build-js",
    "serve": "npm run build && npm run dev",
    "serve-prod": "npm run build-prod && python3 -m http.server 8080 --directory dist",
    "test": "npm run test-e2e",
    "test-e2e": "python3 -m pytest tests/ -v",
    "test-headless": "python3 -m pytest tests/ --headless -v",
    "test-coverage": "python3 -m pytest tests/ --cov --cov-report=html -v",
    "lint": "eslint src/js/ --ext .js",
    "format": "prettier --write src/js/",
    "setup": "npm install && cargo install wasm-pack && playwright install",
    "clean": "rm -rf dist/ pkg/ target/ node_modules/.cache"
  },
  "dependencies": {
    "osmd-audio-player": "^0.7.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@rollup/plugin-commonjs": "^25.0.0",
    "@rollup/plugin-node-resolve": "^15.0.0",
    "@rollup/plugin-terser": "^0.4.0",
    "@unocss/cli": "^66.5.3",
    "eslint": "^9.0.0",
    "globals": "^14.0.0",
    "playwright": "^1.44.0",
    "prettier": "^3.2.0",
    "rollup": "^4.14.0",
    "unocss": "^66.5.3",
    "vite": "^5.2.0",
    "ws": "^8.18.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "keywords": [
    "music",
    "notation",
    "editor",
    "wasm",
    "rust",
    "cell",
    "music-notation"
  ],
  "author": "Editor Project",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/your-org/editor.git"
  },
  "bugs": {
    "url": "https://github.com/your-org/editor/issues"
  },
  "homepage": "https://github.com/your-org/editor#readme"
}
